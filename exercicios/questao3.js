function operacaoExponencial (base, expoente){
    return Math.pow(base, expoente);
}

//let resultado = operacaoExponencial;

console.log(operacaoExponencial(7, 2));
console.log(operacaoExponencial(2, 10));
